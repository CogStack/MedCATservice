#dist: xenial

language: python
version:
  - "3.7"

before_cache:
  - rm -f  $HOME/models/medmen

cache:
  directories:
    - $HOME/models/medmen

before_install:
  - pip install virtualenv

script:
  - ./run_tests.sh
